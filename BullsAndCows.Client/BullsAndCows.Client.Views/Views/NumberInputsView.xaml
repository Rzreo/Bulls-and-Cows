<UserControl x:Class="BullsAndCows.Client.Views.NumberInputsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"  >
    <StackPanel Orientation="Horizontal" Margin="5">
        <ComboBox IsEditable="True" ItemsSource="{Binding Numbers}" SelectedItem="{Binding IN_A}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <prism:InvokeCommandAction Command="{Binding SelectedCommand}" TriggerParameterPath="AddedItems" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ComboBox>
        <ComboBox IsEditable="True" ItemsSource="{Binding Numbers}" SelectedItem="{Binding IN_B}" />
        <ComboBox IsEditable="True" ItemsSource="{Binding Numbers}" SelectedItem="{Binding IN_C}" />
        <Button Background="#666666" Foreground="White" Content="Do" Command="{Binding DoCommand.Cmds}" CommandParameter="{Binding }"/>
        <Button Content="{Binding ViewToggleName }"  Command="{Binding ChangeLogViewCommand}"/>
    </StackPanel>
</UserControl>
